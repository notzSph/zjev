# Export one in the runtime environment. Never commit a real key.
JEV_API_KEY=
JEV_API_PORT=8787
JEV_ENV=production
JEV_API_TOKEN=
JEV_DATABASE_URL=postgresql+psycopg://zjev:change-me@postgres:5432/zjev
POSTGRES_DB=zjev
POSTGRES_USER=zjev
POSTGRES_PASSWORD=change-me
