# Export one in the runtime environment. Never commit a real key.
JEV_API_KEY=
# TYPESAFE_API_KEY=  # accepted as the upstream-compatible alias
JEV_API_PORT=8787
JEV_ENV=production
JEV_API_TOKEN=
JEV_DATABASE_URL=postgresql+psycopg://jevzoo:change-me@postgres:5432/jevzoo
POSTGRES_DB=jevzoo
POSTGRES_USER=jevzoo
POSTGRES_PASSWORD=change-me
